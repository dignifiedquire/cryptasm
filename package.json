{
  "name": "cryptasm",
  "version": "1.0.0",
  "main": "index.js",
  "author": "dignifiedquire <dignifiedquire@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build:debug": "cargo build --target asmjs-unknown-emscripten",
    "build:release": "cargo build --release --target asmjs-unknown-emscripten",
    "bench": "npm run build:release && node bench/index.js",
    "test": "npm run build:debug && jest",
    "dev": "jest --watch"
  },
  "devDependencies": {
    "benchmark": "^2.1.4",
    "browserify-aes": "^1.0.6",
    "file-loader": "^0.11.2",
    "jest": "^20.0.4",
    "rust-wasm-loader": "^0.1.2",
    "webpack": "^3.5.5",
    "webpack-dev-server": "^2.7.1"
  },
  "jest": {
    "testEnvironment": "node",
    "transformIgnorePatterns": [
      "/node_modules/",
      "/target/"
    ]
  }
}
